import { PlaceholderScreen } from "@/src/screens/PlaceholderScreen";

export default function NewTransactionScreen() {
  return (
    <PlaceholderScreen
      title="New Transaction"
      description="Capture income, expense, or transfer details."
    />
  );
}
